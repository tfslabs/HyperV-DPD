<Window x:Class="DDAGUI.CheckForAssignableDevices"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DDAGUI"
        mc:Ignorable="d"
        Title="CheckForAssignableDevices" 
        Height="300" 
        Width="620">
    <Grid Width="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="DeviceList" Grid.Row="0">
            <ListView.Margin>
                <Thickness Top="10"></Thickness>
            </ListView.Margin>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding DeviceName}" Width="120"/>
                    <GridViewColumn Header="Instance Path" DisplayMemberBinding="{Binding DevicePath}" Width="250"/>
                    <GridViewColumn Header="MMIO Space" DisplayMemberBinding="{Binding DeviceMem}" Width="100"/>
                    <GridViewColumn Header="Note" DisplayMemberBinding="{Binding DeviceNote}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" MinWidth="30" Margin="5,0,5,0" Padding="4,1,4,1" VerticalAlignment="Center" Click="Check_Button">Check</Button>
            <Button Grid.Column="2" MinWidth="30" Margin="5,0,5,0" Padding="4,1,4,1" VerticalAlignment="Center" Click="Cancel_Button">Cancel</Button>
        </Grid>
    </Grid>
</Window>
